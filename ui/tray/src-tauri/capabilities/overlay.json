{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-capability",
  "description": "Recording overlay window — only needs invoke access for stop/cancel/drag",
  "windows": ["recording-overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
