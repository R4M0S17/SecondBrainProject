# Docs moved

Project documentation is maintained once at the repo root:

**[`../../docs/README.md`](../../docs/README.md)**

The `docs/` subtree that used to mirror those files here was removed to avoid stale duplicates. Open the link above for the full index.
